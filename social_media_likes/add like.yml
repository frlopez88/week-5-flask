info:
  name: add like
  type: http

http:
  method: POST
  url: http://127.0.0.1:5000/likes
  body:
    type: json
    data: |-
      {
        "user_name" : "hanna", 
        "post_id" : 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
